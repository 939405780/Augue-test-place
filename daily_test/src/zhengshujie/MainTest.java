package zhengshujie;

import java.util.Scanner;

/**
 * 整数解
 * @author Augue
 *
 */
public class MainTest {
	public static void main(String[] args) {
		Scanner scanner = new Scanner(System.in);
		double d;
		while (scanner.hasNext()) {
			int m = scanner.nextInt(); // 和
			int n = scanner.nextInt(); // 积
			if (m == 0 && n == 0) { // 都为0, 结束输入
				break;
			}
			
		}
	}
}
